import React from "react";
import settings from "./Settings.module.css"

const Settings = (props) => {
    return (
        <div className={settings.appWrapperContent}>
            Settings
        </div>
    )
}

export default Settings;