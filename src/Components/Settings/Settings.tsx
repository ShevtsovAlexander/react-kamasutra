import * as React from 'react';
import settings from './Settings.module.css';

const Settings: React.FC = (props) => {
  return <div className={settings.appWrapperContent}>Settings</div>;
};

export default Settings;
